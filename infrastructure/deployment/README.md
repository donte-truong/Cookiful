# Deployment Scaffolding

Cookiful's current deployment target is documented in:

- [Render and GitHub Pages deployment](../../docs/deployment/render-and-github-pages.md)

The active infrastructure entry points are:

- `.github/workflows/deploy-web-pages.yml` for GitHub Pages.
- `render.yaml` for the Render API, Postgres, and Key Value services.

Keep environment-specific notes here when they are operational details rather than product docs.
